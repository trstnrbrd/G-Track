{{-- ============================================================
     Global loading state — top progress bar.
     The styles live in resources/css/app.css (#gt-progress, .gt-spinner)
     and the behavior in resources/js/app.js. This just drops the element.
     Include with: @include('partials.loader')
     ============================================================ --}}
<div id="gt-progress" aria-hidden="true"></div>
